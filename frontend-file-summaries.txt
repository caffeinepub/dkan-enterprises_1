{
  "frontend/src/index.css": "Updated OKLCH color tokens — darkened `--primary` to L=0.42 for readable text on light backgrounds, darkened `--electric-green-dark` to L=0.38 for contrast, added `.font-devanagari` and `.font-poppins` utility aliases, and added `.text-electric`/`.bg-electric`/`.border-electric` legacy aliases so Hero classes resolve correctly.",
  "frontend/tailwind.config.js": "Extended Tailwind config with DKAN brand colors (navy, electric-green, teal), Poppins/Noto Sans Devanagari font families, custom shadows (green-glow, navy-lg), and pulse-green animation.",
  "frontend/src/translations.ts": "Complete bilingual (Hindi/English) translations module; the `t()` helper now accepts both a dot-notation string path AND a `{ hi: string; en: string }` object directly (fixing all 22 TypeScript errors), and adds the previously-missing contact keys `addressValue`, `hoursValue`, `email`, `upGovPartner`, and `mapTitle`.",
  "frontend/src/hooks/useLanguage.ts": "React context-based language hook providing Hindi/English toggle with localStorage persistence and a LanguageProvider wrapper component.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; `useCreateBooking` now correctly unwraps the `Result_2` type from `actor.createBooking()` — throwing an `Error` with the backend's error message when `__kind__ === 'err'` and returning the `bigint` booking ID on success, so the mutation's `onError`/`onSuccess` callbacks fire correctly.",
  "frontend/src/components/Navbar.tsx": "Fixed top navigation bar displaying only the DKAN logo and brand name (SevaMitra logo removed from navbar), desktop nav links, language toggle, and mobile hamburger menu.",
  "frontend/src/components/Hero.tsx": "Hero section with all broken class names fixed — replaced non-existent `text-electric`, `bg-electric`, `border-electric`, `font-poppins`, `font-devanagari` with correct `text-electric-green`, `bg-electric-green`, `border-electric-green`, `font-sans`, `font-hindi` Tailwind classes.",
  "frontend/src/components/SevaMitraBadge.tsx": "Reusable SevaMitra badge component with three variants (hero, footer, inline) that now links to the official UP SevaMitra app on Google Play Store (`https://play.google.com/store/apps/details?id=com.digitalcorn.sewamitra`) instead of the website URL, ensuring users can actually download the app.",
  "frontend/src/components/Services.tsx": "Services section with improved text contrast — replaced `text-electric-green`/`text-electric-green-dark` badge and price text with `text-primary` (dark teal, L=0.42) for legibility on white/light card backgrounds, and changed icon colors from `*-400` to `*-600` for better contrast.",
  "frontend/src/components/BookingForm.tsx": "Public booking form with full validation and bilingual labels; fixed to use `t()` translation keys for all messages, and the `onSuccess`/`onError` callbacks now correctly receive a `bigint` ID or a typed `Error` (since `useCreateBooking` properly unwraps the `Result_2` response), eliminating spurious error messages on successful bookings.",
  "frontend/src/components/Testimonials.tsx": "Testimonials section with improved contrast — replaced `text-electric-green` badge/icon with `text-primary`, changed review text from `text-foreground/80` to `text-foreground`, updated star color to `yellow-500`, avatar backgrounds to `*-600` variants, and service badge to use `text-primary`.",
  "frontend/src/components/Contact.tsx": "Contact section with improved icon and label contrast — changed icon colors from `*-500` to `*-700` and backgrounds from `*-500/10` to `*-100` for better visibility, replaced `text-electric-green` with `text-primary`, and upgraded label from `font-medium` to `font-semibold` for clearer readability.",
  "frontend/src/components/WhatsAppButton.tsx": "Fixed floating WhatsApp chat button in bottom-right corner linking to wa.me/918009675645 with WhatsApp SVG icon and bilingual label.",
  "frontend/src/components/Footer.tsx": "Multi-column dark navy footer with DKAN logo, SevaMitra footer badge, UP Government badge, quick links, live contact info from settings, and caffeine.ai attribution.",
  "frontend/src/App.tsx": "Root app with TanStack Router; AdminPage now correctly re-renders when authentication state changes by using a useCallback-stable handler, and AdminPasswordGate is shown until both password and Internet Identity are verified.",
  "frontend/index.html": "SEO-optimized HTML entry point with bilingual meta tags, Open Graph data, and Google Fonts preconnect for DKAN Enterprises.",
  "frontend/src/components/AdminPasswordGate.tsx": "Two-step admin authentication gate — first verifies a frontend password, then requires Internet Identity login so the backend actor has admin privileges to call getAllBookings(). Shows appropriate loading/error states and a back button between steps.",
  "frontend/src/pages/HomePage.tsx": "New HomePage component that renders all homepage sections (Hero, Services, BookingForm, Testimonials, Contact, WhatsAppButton) for the root route.",
  "frontend/src/pages/AdminDashboard.tsx": "Full admin dashboard with Bookings/Services/Settings tabs; fixes the TypeScript error by extracting a typed `settingsFields` array with `key: SettingsKey` so `form[key]` is accessed with proper `keyof Settings` typing instead of an unsafe cast to `Record<string, string>`."
}